name: "Test: Run two"

on:
  push:
    branches:
      - main
    paths:
      - two/**
    

jobs:
  two:
    needs: ${{ github.event.workflow_run.conclusion == 'success' && needs.one }} # Conditional needs
    runs-on: ubuntu-latest
    name: "Run when there are changes to Two"
    steps:
      - name: "Echo Two"
        run: echo 'Two runs'